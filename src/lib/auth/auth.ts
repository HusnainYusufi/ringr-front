// Better-Auth removed — Ringr uses its own NestJS JWT backend.
// This stub prevents build errors from other template files that import this module.
export const auth = { api: { getSession: async () => null } };
